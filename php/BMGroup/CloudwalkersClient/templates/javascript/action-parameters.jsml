<h3>{{title}}</h3>

<form>
	{{#input.string}}
		<div class="form-group">
			<label>{{name}}</label>
			<input type="text" name="{{token}}" value="{{value}}" />
		</div>
	{{/input.string}}

	<button type="submit" class="btn">{{action.name}}</button>
</form>