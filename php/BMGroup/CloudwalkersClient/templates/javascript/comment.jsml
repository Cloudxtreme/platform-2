{{#comment.from}}
	<img class="avatar" alt="" src="{{avatar}}">
{{/comment.from}}

<span>
	{{#comment.from}}<strong class='title'>{{name}}</strong>{{/comment.from}}
	<span class="date time time-since" data-date="{{comment.date}}">{{comment.date}}</span>
	<div class='content'>
		{{{comment.body.html}}}
	</div>
</span>