<table class="table table-striped table-bordered table-hover charttable">

	<thead>
		<tr>
			{{#header}}
				<th class="{{class}}">
					{{name}}
				</th>
			{{/header}}
		</tr>
	</thead>

	<tbody>
		{{#rows}}

			<tr>
				{{#columns}}
					<td class="{{class}}">
						{{value}}
					</td>
				{{/columns}}
			</tr>

		{{/rows}}
	</tbody>

</table>