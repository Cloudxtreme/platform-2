<li>
	<input type="checkbox" name="{{channel.name}}" {{#selected}}checked="checked"{{/selected}} data-stream-id="{{stream.id}}" data-channel-id="{{channel.id}}" />
	{{channel.name}}


	{{! Recursive template is currently not set. Go back in the revision to see what to do to add it again. But basically, loop trough channels and include this template again. }}
</li>